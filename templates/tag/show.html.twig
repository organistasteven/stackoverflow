{% extends 'base.html.twig' %}

{% block title %}Tag{% endblock %}

{% block body %}
    <h1>Tag</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tag.id }}</td>
            </tr>
            <tr>
                <th>Libellé</th>
                <td>{{ tag.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tag_edit', {'id': tag.id}) }}" class="btn btn-success mr-2">Modifier</a><!-- 
    --><a href="{{ path('tag_index') }}" class="btn btn-primary">Retour à la liste</a>

    {{ include('tag/_delete_form.html.twig') }}
{% endblock %}