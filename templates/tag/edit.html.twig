{% extends 'base.html.twig' %}

{% block title %}Modifier un Tag{% endblock %}

{% block body %}
    <h1>Modifier un Tag</h1>

    {{ include('tag/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('tag_index') }}" class="btn btn-primary">Retour à la liste</a>

    {{ include('tag/_delete_form.html.twig') }}
{% endblock %}